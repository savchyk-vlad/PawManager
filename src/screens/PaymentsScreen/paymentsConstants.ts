/** Comfortable minimum tap target (~44pt iOS / ~48dp Material). */
export const PAYMENTS_MIN_TOUCH = 44;

/** Monthly earnings chart + revenue insights — flip on for next release. */
export const SHOW_PAYMENTS_MONTHLY_AND_INSIGHTS = false;
